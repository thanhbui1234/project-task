const PUBLIC_PATH = {
  LOGIN: '/',
  REGISTER: '/register',
};

const PRIVATE_PATH = {
  DASHBOARD: '/dashboard',
  PROJECT: '/project',
};

const URL_PATH = {
  ...PUBLIC_PATH,
  ...PRIVATE_PATH,
};

export { URL_PATH };
